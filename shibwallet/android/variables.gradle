ext {
    minSdkVersion = 24
    compileSdkVersion = 36
    targetSdkVersion = 34
    androidxActivityVersion = '1.11.0'
    androidxAppCompatVersion = '1.7.1'
    androidxCoordinatorLayoutVersion = '1.3.0'
    androidxCoreVersion = '1.17.0'
    androidxFragmentVersion = '1.8.9'
    coreSplashScreenVersion = '1.2.0'
    androidxWebkitVersion = '1.14.0'
    junitVersion = '4.13.2'
    androidxJunitVersion = '1.3.0'
    androidxEspressoCoreVersion = '3.7.0'
    cordovaAndroidVersion = '14.0.1'
    // Kotlin support added for the Tangem hardware-wallet plugin.
    kotlinVersion = '2.0.21'
    // Tangem SDK via GitHub Packages (Tangem doesn't publish to JitPack despite the badge).
    // To build, the project needs read:packages access for github.com/tangem/tangem-sdk-android,
    // supplied as `gpr.user` + `gpr.key` in ~/.gradle/gradle.properties OR
    // `GITHUB_ACTOR` + `GITHUB_TOKEN` env vars. See android/build.gradle repositories block.
    tangemSdkVersion = 'releases-5.37-608'
}